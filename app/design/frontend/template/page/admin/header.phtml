<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Dashboard</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
  <link rel="stylesheet" href="styles.css" />
</head>

<body>
  <header class="admin-header">
    <div class="logo">Admin Panel</div>
    <nav>
      <ul>
        <li><a href="<?php echo $this->getUrl("admin/dashboard/list") ?>"><i class="fa-solid fa-chart-line"></i> Dashboard</a></li>
        <?php if ($this->isAllowed('product')): ?>
          <li><a href="<?php echo $this->getUrl("admin/product_index/list") ?>"><i class="fa-solid fa-box"></i> Products</a></li><?php endif; ?>
        <?php if ($this->isAllowed('category')): ?>
          <li><a href="<?php echo $this->getUrl("admin/category/list") ?>"><i class="fa-solid fa-list"></i> Categories</a></li><?php endif; ?>
        <?php if ($this->isAllowed('order')): ?>
          <li><a href="<?php echo $this->getUrl("admin/order/list") ?>"><i class="fa-solid fa-shopping-cart"></i> Orders</a></li><?php endif; ?>
          <li><a href="<?php echo $this->getUrl("admin/replay_Index/list") ?>">Replay</a></li>
        <li><?php if (is_null($this->getLogin())): ?><a href="<?php echo $this->getUrl("admin/account/login") ?>"><i class="fa-solid fa-right-to-bracket"></i> Login</a>
          <?php else: ?><a href="<?php echo $this->getUrl("admin/account/logout") ?>"><i class="fa-solid fa-right-to-bracket"></i> Logout</a>
          <?php endif ?>
        </li>
      </ul>
    </nav>
  </header>

  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
    }

    .admin-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #1e293b;
      padding: 15px 30px;
      color: white;
    }

    .logo {
      font-size: 1.5rem;
      font-weight: bold;
    }

    nav ul {
      list-style-type: none;
      margin: 0;
      padding: 0;
      display: flex;
    }

    nav ul li {
      margin-left: 20px;
    }

    nav ul li a {
      text-decoration: none;
      color: white;
      font-size: 1rem;
      display: flex;
      align-items: center;
    }

    nav ul li a i {
      margin-right: 8px;
    }

    nav ul li a:hover {
      color: #facc15;
    }
  </style>
</body>

</html>