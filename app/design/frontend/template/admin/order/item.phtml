<?php
// item.phtml - Updated with proper styling and responsive data attributes
$product = $this->getItems();
$base_url = Mage::getBaseUrl();
?>

<div class="section order-items">
    <h3>Order Items</h3>
    <div class="table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Product ID</th>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Color</th>
                    <th>Quantity</th>
                    <th>Unit Price</th>
                    <th>Subtotal</th>
                </tr>
            </thead>
            <tbody>
                <?php for ($i = 0; $i < count($product); $i++): ?>
                    <?php $image_path = $base_url . $product[$i]->getProduct()->getFilePath()['main_image']; ?>
                    <tr>
                        <td data-label="Image"><img src="<?php echo $image_path; ?>" height="80" width="80" alt="Product Image"></td>
                        <td data-label="Product ID"><?php echo $product[$i]->getProduct()->getProductId(); ?></td>
                        <td data-label="Name"><?php echo $product[$i]->getProduct()->getName(); ?></td>
                        <td data-label="Description"><?php echo $product[$i]->getProduct()->getDescription(); ?></td>
                        <td data-label="Color"><?php echo $product[$i]->getProduct()->getColor(); ?></td>
                        <td data-label="Quantity"><?php echo $product[$i]->getProductQuantity(); ?></td>
                        <td data-label="Unit Price"><?php echo number_format($product[$i]->getPrice(), 2); ?></td>
                        <td data-label="Subtotal"><?php echo number_format($product[$i]->getProductQuantity() * $product[$i]->getPrice(), 2); ?></td>
                    </tr>
                <?php endfor; ?>
            </tbody>
        </table>
    </div>
</div>